name: Build and Deploy

on:
  push:
    branches: [ "main" ]
env:
  IMAGE_NAME: hello-node
  REGISTRY: docker.io/joaopedroimorais
  OPENSHIFT_PROJECT: joaopedroigeski-dev

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Login no Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}

      - name: Build da imagem
        run: |
          docker build -t $REGISTRY/$IMAGE_NAME:latest .

      - name: Push da imagem
        run: |
          docker push $REGISTRY/$IMAGE_NAME:latest

      - name: Install oc
        uses: redhat-actions/oc-installer@v1
        with:
          oc_version: '4.6'

      - name: Login no OpenShift
        run: |
          oc login ${{ secrets.OPENSHIFT_SERVER }} \
            --token=${{ secrets.OPENSHIFT_TOKEN }} \
            --insecure-skip-tls-verify=true

      - name: Selecionar projeto sandbox
        run: |
          oc project $OPENSHIFT_PROJECT

      - name: Atualizar deployment
        run: |
          oc set image hello-world-node-js/hello-node hello-node=$REGISTRY/$IMAGE_NAME:latest || \
          oc rollout restart hello-world-node-js/hello-node
